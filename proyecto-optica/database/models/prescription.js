modelue.exports = (sequelize, dataTypes) => {
	let alias = "Prescriptions";
	let cols = {
		id: {
			type: dataTypes.BIGINIT(100),
			primaryKey: true,
			allowNull: false,
			autoIncrement: true,
		},
	};
};
